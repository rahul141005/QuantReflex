# QuantReflex — Technical Debt Inventory (ADR-138)

**Generated by `main-app/scripts/dev/tech-debt-inventory.js`. This document changes nothing.**

Produced during the ADR-138 release certification, where the explicit rule was: the certification branch
carries regression fixes, UI corrections, gate improvements and production-readiness work **only** — no
removals, no consolidation, no refactors. Everything below is the backlog for a separate Code Cleanup
milestone, where each item can be removed individually with its own regression test.

**Read the confidence column before deleting anything.** A literal-absence scan is not proof of death in
this codebase: `.is-error` and `.strength-weak` are never written literally — they are built by
`'is-' + type` and `'strength-' + level`. ADR-133 hit the mirror image: `--text-secondary` looked
dead in CSS and was read by `js/views/inbox-view.js`. Every candidate here is checked for literal use,
dynamic construction from any prefix JS concatenates, and template interpolation.

| Grade | Meaning |
|---|---|
| `REVIEW` | No reference found by any of the three methods. Still requires a human read before removal. |
| `LOW` | No literal reference, but a JS concatenation prefix could construct it at runtime. **Assume alive.** |
| `INFO` | Not dead — a structural observation (duplication, magic numbers) for the cleanup milestone. |

---

## 1 · CSS classes with no discoverable reference

1389 classes declared in `css/style.css`; 80 have no literal reference.

| Class | Confidence | Evidence |
|---|---|---|
| `accent-amber` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "accent-" — could be constructed at runtime |
| `accent-blue` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "accent-" — could be constructed at runtime |
| `accent-green` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "accent-" — could be constructed at runtime |
| `accent-rose` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "accent-" — could be constructed at runtime |
| `accent-slate` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "accent-" — could be constructed at runtime |
| `ai-coach-card` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `ai-coach-locked` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `ai-spinner` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `ai-spinner-inline` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `ai-study-plan-body` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `cat-name` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `coaching-visible` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `d-easy` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "d-" — could be constructed at runtime |
| `d-hard` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "d-" — could be constructed at runtime |
| `d-medium` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "d-" — could be constructed at runtime |
| `di-series-1` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "di-series-" — could be constructed at runtime |
| `di-series-2` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "di-series-" — could be constructed at runtime |
| `di-series-3` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "di-series-" — could be constructed at runtime |
| `di-series-4` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "di-series-" — could be constructed at runtime |
| `di-series-5` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "di-series-" — could be constructed at runtime |
| `duel-invite-countdown` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-mode-btn` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-player-card` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-player-connection-dot` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-player-slot` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-premium-badge` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-premium-notice` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-result-comparison` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-result-player` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-result-title` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-results-card` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-username-status` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `duel-waiting-indicator` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `goal-card` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `goal-progress-fill` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `home-bento-duel-live` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `home-streak-badge` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `is-error` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "is-" — could be constructed at runtime |
| `is-info` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "is-" — could be constructed at runtime |
| `is-success` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "is-" — could be constructed at runtime |
| `is-upcoming` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "is-" — could be constructed at runtime |
| `k-bf` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "k-" — could be constructed at runtime |
| `k-mk` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "k-" — could be constructed at runtime |
| `k-ms` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "k-" — could be constructed at runtime |
| `k-rs` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "k-" — could be constructed at runtime |
| `k-rv` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "k-" — could be constructed at runtime |
| `k-st` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "k-" — could be constructed at runtime |
| `kind-dismiss` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "kind-" — could be constructed at runtime |
| `kind-drill` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "kind-" — could be constructed at runtime |
| `login-field` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `login-input` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `mode-card-duel` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `nav-link` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `numpad-key` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `paywall-plus-toggle-btn` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `pm-dot` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `pm-name` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `profile-password-wrapper` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `ps-custom` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-badge-pro` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-btn-block` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-btn-ghost` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-btn-lg` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-btn-sm` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-btn-tonal` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-segment` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-segment-btn` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-skel` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `qr-skel-row` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `settings-group` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `shortcut-table` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |
| `strength-moderate` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "strength-" — could be constructed at runtime |
| `strength-strong` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "strength-" — could be constructed at runtime |
| `strength-weak` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "strength-" — could be constructed at runtime |
| `tone-info` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "tone-" — could be constructed at runtime |
| `tone-success` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "tone-" — could be constructed at runtime |
| `tr-down` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "tr-" — could be constructed at runtime |
| `tr-flat` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "tr-" — could be constructed at runtime |
| `tr-up` | LOW — may be built dynamically | no literal occurrence, but JS concatenates the prefix "tr-" — could be constructed at runtime |
| `warning` | REVIEW — no literal reference found | no occurrence in index.html or any js/**/*.js, literal or concatenated |


## 2 · Custom properties declared but never read

222 custom properties declared.

| Token | Confidence | Evidence |
|---|---|---|
| `--sp-10` | KEEP — reserved by design | unused step of the canonical 4px scale; exempted by design-lint (ADR-134) on purpose — do not remove |
| `--sp-12` | KEEP — reserved by design | unused step of the canonical 4px scale; exempted by design-lint (ADR-134) on purpose — do not remove |
| `--sp-8` | KEEP — reserved by design | unused step of the canonical 4px scale; exempted by design-lint (ADR-134) on purpose — do not remove |


## 3 · Selectors declared more than twice

Not necessarily wrong — media-query overrides legitimately repeat a selector. Listed so the cleanup
milestone can judge each on its merits.

| Selector | Grade | Evidence |
|---|---|---|
| `.container` | INFO | 6 declarations at lines 494, 3523, 3526, 3534, 3535, 4122 |
| `body.view-learn-active .container` | INFO | 5 declarations at lines 8879, 8888, 8889, 8890, 8891 |
| `.kx-topic-grid` | INFO | 4 declarations at lines 8929, 8931, 8932, 8933 |
| `.pd-cell` | INFO | 4 declarations at lines 8455, 8646, 8663, 8668 |
| `.question-text` | INFO | 4 declarations at lines 678, 3522, 4084, 4098 |
| `body.drill-session-active` | INFO | 4 declarations at lines 3839, 4088, 4100, 9956 |
| `.mcq-option` | INFO | 3 declarations at lines 9421, 9450, 9458 |
| `.mcq-options` | INFO | 3 declarations at lines 9412, 9421, 9457 |
| `.duel-result-score` | INFO | 3 declarations at lines 6323, 8181, 8228 |
| `.duel-share-btn` | INFO | 3 declarations at lines 6194, 8034, 8198 |
| `body.drill-session-active .duel-solve-header` | INFO | 3 declarations at lines 6128, 8200, 8204 |
| `header h1` | INFO | 3 declarations at lines 519, 3521, 4120 |
| `header` | INFO | 3 declarations at lines 508, 4095, 4119 |
| `.spa-view-active` | INFO | 3 declarations at lines 474, 491, 5842 |
| `.numpad-btn` | INFO | 3 declarations at lines 3778, 4086, 4099 |
| `.analytics-card` | INFO | 3 declarations at lines 3491, 3516, 5453 |
| `.practice-container .mode-card p` | INFO | 3 declarations at lines 3317, 4116, 7932 |
| `.practice-container .mode-card` | INFO | 3 declarations at lines 3310, 4093, 4112 |
| `.tables-container` | INFO | 3 declarations at lines 1152, 1199, 3527 |
| `:root` | INFO | 3 declarations at lines 1, 9818, 10128 |


## 4 · `!important` usage

**58** declarations, at lines: 317, 344, 2133, 2134, 2135, 2136, 2137, 2138, 2140, 2146, 2147, 2148, 2149, 2150, 2151, 2153, 2154, 2155, 2156, 2157, 3653, 5616, 5617, 5619, 5620, 5630, 5631, 5840, 5844, 5847, 5848, 5908, 5978, 5980, 6574, 8677, 8878, 9041, 9042, 9043, 9044, 9135, 9136, 9137, 9273, 9274, 9275, 9285, 9286, 9684, 9687, 9688, 9719, 9721, 10215, 10216, 10218, 10219

Most are the reduced-motion kill-switch and the Playful icon-language overrides, both of which need to
win by design. The cleanup milestone should confirm that each remaining one still does.

## 5 · Inline styles in `index.html`

**84** `style="…"` attributes. Already censused and ceiling-capped by
`design-lint.check.js` (ADR-135) so no NEW inline duration, easing, shadow, z-index or font-size can be
added — the cleanup itself remains deferred by explicit instruction.

## 6 · Off-scale px literals in `css/style.css`

The spacing scale is `[2, 4, 8, 12, 16, 20, 24, 28, 32, 36, 40, 44, 48]` (ADR-133). Values outside it
are not automatically wrong — shadows, blurs, hairlines and glyph metrics legitimately sit off-grid —
but the frequent ones are worth a look.

| Value | Occurrences |
|---|---|
| 3px | 42 |
| 5px | 26 |
| 6px | 16 |
| 600px | 12 |
| 10px | 11 |
| 640px | 10 |
| 9px | 9 |
| 320px | 8 |
| 960px | 8 |
| 7px | 7 |
| 420px | 7 |
| 120px | 6 |
| 280px | 6 |
| 400px | 6 |
| 480px | 6 |
| 720px | 6 |
| 14px | 5 |
| 26px | 5 |
| 60px | 5 |
| 768px | 5 |
| 18px | 4 |
| 50px | 4 |
| 56px | 4 |
| 80px | 4 |
| 340px | 4 |

---

_Regenerate with `node scripts/dev/tech-debt-inventory.js --write` from `main-app/`._
